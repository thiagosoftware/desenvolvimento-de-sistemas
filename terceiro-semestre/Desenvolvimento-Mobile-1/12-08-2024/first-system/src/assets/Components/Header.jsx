const Header = () => {
    console.log('Header is rendering'); // Verifica se o Header está sendo renderizado

    return (
        <header style={{ background: 'red' }}>
            <h1>First System</h1>
            <p>Lorem ipsum</p>
        </header>
    );
};

export default Header;
